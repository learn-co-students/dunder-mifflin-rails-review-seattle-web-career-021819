<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Employee Form</title>
  </head>
  <body>
    <% if @employee.errors.any?  %>
      <div id="error explanation">
      <h2>We found a few errors in your form:</h2>
      <ul>
      <% @employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end  %>
      </ul>
    <% end  %>
    <label><h1>Edit Employee</h1></label>
    <ul>
      <%= form_for @employee do |f| %>
      <h3><%= f.label :first_name %></h3>
      <%= f.text_field :first_name %><br>
      <h3><%= f.label :last_name %></h3>
      <%= f.text_field :last_name %><br>
      <h3><%= f.label :alias %></h3>
      <%= f.text_field :alias%><br>
      <h3><%= f.label :title %></h3>
      <%= f.text_field :title %><br>
      <h3><%= f.label :office %></h3>
      <%= f.text_field :office %><br>
      <h3><%= f.label :img_url %></h3>
      <%= f.text_field :img_url %><br>
      <%= f.collection_select :dog_id, Dog.all, :id, :name %>
      <%= f.submit "Edit Employee" %>
      <% end %>
    </ul>
  </body>
</html>
